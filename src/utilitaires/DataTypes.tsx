export type classes = {
    id: number;
    nomClasse: string;
    appelation: string;
    ordreEnseignement: string;}

    export type classeForm = {
    nomClasse: string;
    appelation: string;
    ordreEnseignement: string;}

    export type anneesScolaires = {
        id?: number;
        anneeScolaire: string;
        active?: boolean;
    }